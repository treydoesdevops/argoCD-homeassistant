apiVersion: v1
kind: Service
metadata:
  name: homst-assistant
  namespace: home
spec:
  selector:
    app: home-assistant
  ports:
    - protocol: TCP
      port: 8123
      targetPort: 8123
  type: ClusterIP